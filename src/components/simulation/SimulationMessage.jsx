import {motion}from'motion/react';
export function SimulationMessage({message}){return <motion.article className={'debate-message '+message.sentiment} initial={{opacity:0,y:14,filter:'blur(5px)'}} animate={{opacity:1,y:0,filter:'blur(0)'}}><div className="message-avatar">{message.name.split(' ').map(x=>x[0]).join('')}</div><div><header><b>{message.name}</b><small>{message.age} · {message.role} · {message.income} income</small><em>{message.sentiment.toUpperCase()} · {message.confidence}%</em></header><div className="persona-facts">{message.behaviour} · {message.personality} · {message.probability}% buy likelihood</div><p>{message.text}</p><span className="emotion-badge">{message.emotion} · {message.topic}</span></div></motion.article>}
