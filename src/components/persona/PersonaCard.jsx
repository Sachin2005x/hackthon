import { motion } from 'motion/react';
export function PersonaCard({persona,onSelect,index}){return <motion.button className="persona-detail-card" onClick={()=>onSelect(persona)} aria-label={`Open ${persona.name}'s persona profile`} initial={{opacity:0,y:22,scale:.96,filter:'blur(7px)'}} animate={{opacity:1,y:0,scale:1,filter:'blur(0)'}} transition={{delay:index*.035,duration:.42}} whileHover={{y:-8,rotateX:3,rotateY:-3}}><i className="detail-avatar">{persona.name.split(' ').map(x=>x[0]).join('')}</i><div className="card-id">PERSONA_{String(persona.id).padStart(3,'0')}</div><h3>{persona.name}</h3><p>{persona.age} · {persona.gender} · {persona.country}</p><span>{persona.profession}</span><div className="detail-pills"><b>{persona.behaviour}</b><b>{persona.income} income</b></div><div className="detail-score"><span>Purchase probability</span><strong>{persona.purchaseProbability}%</strong></div><div className="detail-score"><span>AI confidence</span><strong>{persona.confidence}%</strong></div></motion.button>}
