import {useEffect,useState}from'react';import{simulationMessages}from'../data/simulationData';
export function useSimulation(){const[running,setRunning]=useState(true),[speed,setSpeed]=useState(1),[items,setItems]=useState(simulationMessages.slice(0,3)),[cursor,setCursor]=useState(3),[processed,setProcessed]=useState(1284),[typing,setTyping]=useState(false);useEffect(()=>{if(!running)return;const delay=3000/speed;const id=setInterval(()=>{setTyping(true);setTimeout(()=>{setItems(x=>[...x.slice(-11),simulationMessages[cursor%simulationMessages.length]]);setCursor(x=>x+1);setProcessed(x=>x+1);setTyping(false)},Math.max(300,850/speed))},delay);return()=>clearInterval(id)},[running,speed,cursor]);return{running,setRunning,speed,setSpeed,items,processed,typing,restart:()=>{setItems(simulationMessages.slice(0,3));setCursor(3);setProcessed(1284)}}}
